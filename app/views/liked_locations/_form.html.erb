<%= form_with(model: liked_location, local: true) do |form| %>
  <% if liked_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(liked_location.errors.count, "error") %> prohibited this liked_location from being saved:</h2>

      <ul>
        <% liked_location.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :location_id %>
    <%= form.text_field :location_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
