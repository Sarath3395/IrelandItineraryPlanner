<%= form_with(model: liked_hotel, local: true) do |form| %>
  <% if liked_hotel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(liked_hotel.errors.count, "error") %> prohibited this liked_hotel from being saved:</h2>

      <ul>
        <% liked_hotel.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :hotel_id %>
    <%= form.text_field :hotel_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
