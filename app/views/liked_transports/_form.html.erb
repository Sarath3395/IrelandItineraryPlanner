<%= form_with(model: liked_transport, local: true) do |form| %>
  <% if liked_transport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(liked_transport.errors.count, "error") %> prohibited this liked_transport from being saved:</h2>

      <ul>
        <% liked_transport.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :transport_id %>
    <%= form.text_field :transport_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
